server:
  port: 8080
  servlet:
    contextPath: /v1/api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/giffunding?createDatabaseIfNotExist=true
    username: root
    password: 1234
    hikari:
      auto-commit: false
      connection-test-query: SELECT 1
      minimum-idle: 10
      maximum-pool-size: 20
      pool-name: giffunding-user-pool

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        hbm2ddl.auto: create
    open-in-view: false
    show-sql: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  mvc:
    static-path-pattern: /static/**

logging.level:
  org.hibernate.SQL: debug

kakao:
  client-id: YOUR_KAKAO_CLIENT_ID
  client-secret: YOUR_KAKAO_CLIENT_SECRET
  redirect-uri: YOUR_REDIRECT_URI
  token-uri: https://kauth.kakao.com/oauth/token
  user-info-uri: https://kapi.kakao.com/v2/user/me

cloud:
  aws:
    s3:
      bucket: giffunding
    credentials:
      access-key: AKIA3YKYKRYVFRDYDTVS
      secret-key: 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
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false

jwt:
  secret: 3H4e3DlTawx8T0Ti79PKCeDhHngmQbuiUeSy80qAGn8=
